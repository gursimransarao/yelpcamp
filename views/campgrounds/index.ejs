<% layout('layouts/boilerplate')%>
<div id="cluster-map" class="mb-3"></div>
<!-- <div class="">
  <div>
    <h1 class="float-md-start">All Campgrounds</h1>

    <a href="/campgrounds/new" class="btn btn-outline-secondary float-md-end"
      >Add Campground</a
    >
  </div>
</div> -->
<nav class="navbar mb-3">
  <div class="container-fluid">
    <h1 class="">All Campgrounds</h1>
    <a href="/campgrounds/new" class="btn btn-outline-secondary"
      >Add Campground</a
    >
  </div>
</nav>
<% for (let campground of campgrounds) {%>
<div class="card mb-3">
  <div class="row">
    <div class="col-md-4">
      <% if(campground.images.length) { %>
      <img class="img-fluid" alt="" src="<%= campground.images[0].url %>" />
      <% } else { %>
      <img
        class="img-fluid"
        alt=""
        src="https://res.cloudinary.com/dvuwlh8op/image/upload/v1705145092/YelpCamp/Screenshot_2024-01-13_165207_kzwvug.png"
      />

      <% } %>
    </div>
    <div class="col-md-8 mb-3 mt-3">
      <h5 class="card-title"><%= campground.title %></h5>

      <p class="card-text"><%= campground.description %></p>
      <p class="card-text">
        <small class="text-muted"><%= campground.location %></small>
      </p>
      <a href="/campgrounds/<%= campground._id %>" class="btn btn-primary"
        >View <%= campground.title %></a
      >
    </div>
  </div>
</div>

<% } %>
<script>
  const mapToken = "<%-process.env.MAPBOX_TOKEN%>";
  const campgrounds = {features:<%- JSON.stringify(campgrounds)%>};
</script>

<script src="/javascripts/clusterMap.js"></script>
